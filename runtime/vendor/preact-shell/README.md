# Vendored preact-shell source

This directory mirrors selected source packages from `cjnova/preact-shell` for PiClaw integration work:

- `ui`
- `auth-provider`
- `identity-contracts`
- `workspace-provider`

Use `scripts/refresh-vendored-preact-shell-artifacts.sh` from repo root to refresh from a local preact-shell checkout.
